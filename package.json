{
  "name": "taskpoints-dashboard",
  "private": true,
  "version": "0.2.0",
  "description": "TaskPoints PRO com deploy em Vercel e persistencia em Supabase",
  "scripts": {
    "build": "node scripts/generate-runtime-config.cjs",
    "build:runtime-config": "node scripts/generate-runtime-config.cjs",
    "dev": "npx live-server . --port=5500",
    "db:push": "supabase db push",
    "migrate:indexeddb:to:postgres": "node scripts/migrate-indexeddb-backup-to-postgres.cjs",
    "test:app": "node scripts/verify-st011.cjs && node scripts/test-st011-behavior.cjs"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
